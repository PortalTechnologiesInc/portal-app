appId: cc.getportal.portal
tags:
  - camera
  - wallet
---
# Camera flow: scan a QR code for wallet connection (NWC).
# TODO: implement — requires a logged-in user and a valid NWC QR payload.
- launchApp
